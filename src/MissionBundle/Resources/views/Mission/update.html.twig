{% extends "::base.html.twig" %}
{% block body%}
    <div class="container" style="margin-top: 30px">
        <div class="row">
            <h2>Page Update</h2>
            <div class="col-md-8">
                <form action="{{ path('mission_mission_page_update', {'missionId': mission.id}) }}" method="get">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <div class="form-group">
                        <input name="serviceDate" type="datetime-local" class="form-control">
                    </div>
                    <input type="submit" class="btn btn-success">Update
                    {{ form_end(form) }}
                </form>
            </div>
        </div>
    </div>
{% endblock %}